import { ResponseError } from './types';
import './ContentAnalyticsErrorState.scss';
interface Props {
    error?: ResponseError;
}
declare const ContentAnalyticsErrorState: ({ error }: Props) => JSX.Element;
export default ContentAnalyticsErrorState;
